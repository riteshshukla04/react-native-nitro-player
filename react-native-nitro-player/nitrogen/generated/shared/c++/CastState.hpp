///
/// CastState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroplayer {

  /**
   * An enum which can be represented as a JavaScript union (CastState).
   */
  enum class CastState {
    NO_DEVICES_AVAILABLE      SWIFT_NAME(noDevicesAvailable) = 0,
    NOT_CONNECTED      SWIFT_NAME(notConnected) = 1,
    CONNECTING      SWIFT_NAME(connecting) = 2,
    CONNECTED      SWIFT_NAME(connected) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroplayer

namespace margelo::nitro {

  // C++ CastState <> JS CastState (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroplayer::CastState> final {
    static inline margelo::nitro::nitroplayer::CastState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("no_devices_available"): return margelo::nitro::nitroplayer::CastState::NO_DEVICES_AVAILABLE;
        case hashString("not_connected"): return margelo::nitro::nitroplayer::CastState::NOT_CONNECTED;
        case hashString("connecting"): return margelo::nitro::nitroplayer::CastState::CONNECTING;
        case hashString("connected"): return margelo::nitro::nitroplayer::CastState::CONNECTED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum CastState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroplayer::CastState arg) {
      switch (arg) {
        case margelo::nitro::nitroplayer::CastState::NO_DEVICES_AVAILABLE: return JSIConverter<std::string>::toJSI(runtime, "no_devices_available");
        case margelo::nitro::nitroplayer::CastState::NOT_CONNECTED: return JSIConverter<std::string>::toJSI(runtime, "not_connected");
        case margelo::nitro::nitroplayer::CastState::CONNECTING: return JSIConverter<std::string>::toJSI(runtime, "connecting");
        case margelo::nitro::nitroplayer::CastState::CONNECTED: return JSIConverter<std::string>::toJSI(runtime, "connected");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert CastState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("no_devices_available"):
        case hashString("not_connected"):
        case hashString("connecting"):
        case hashString("connected"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
