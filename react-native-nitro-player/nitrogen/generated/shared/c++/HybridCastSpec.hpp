///
/// HybridCastSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `CastState` to properly resolve imports.
namespace margelo::nitro::nitroplayer { enum class CastState; }

#include <NitroModules/Promise.hpp>
#include <string>
#include <optional>
#include "CastState.hpp"
#include <NitroModules/Null.hpp>
#include <variant>
#include <functional>

namespace margelo::nitro::nitroplayer {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Cast`
   * Inherit this class to create instances of `HybridCastSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridCast: public HybridCastSpec {
   * public:
   *   HybridCast(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridCastSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridCastSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridCastSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> configure(const std::optional<std::string>& receiverApplicationId) = 0;
      virtual bool isCasting() = 0;
      virtual CastState getCastState() = 0;
      virtual std::variant<nitro::NullType, std::string> getCastDeviceName() = 0;
      virtual void showCastPicker() = 0;
      virtual std::shared_ptr<Promise<void>> endCastSession() = 0;
      virtual void onCastStateChange(const std::function<void(CastState /* state */, const std::optional<std::variant<nitro::NullType, std::string>>& /* deviceName */)>& callback) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Cast";
  };

} // namespace margelo::nitro::nitroplayer
