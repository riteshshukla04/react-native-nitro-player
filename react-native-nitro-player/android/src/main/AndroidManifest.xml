<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions for downloads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Google Cast device discovery (mDNS / Cast SDK) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Required so Android Auto / Assistant treat this as a media app. -->
    <uses-feature
        android:name="android.hardware.audio.output"
        android:required="false" />

    <application>
        <!-- Google Cast: supplies CastOptions (receiver app ID) to the Cast SDK.
             Apps shipping their own OptionsProvider must override with tools:replace. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.margelo.nitro.nitroplayer.media.NitroCastOptionsProvider" />

        <!-- PlaybackService keeps music alive when screen is locked / app backgrounded -->
        <service
            android:name="com.margelo.nitro.nitroplayer.media.NitroPlayerPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:node="merge">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
            <!-- Legacy voice-search entry point. Modern Android Auto / Assistant
                 routes through MediaSession.Callback.onSetMediaItems with a
                 searchQuery, but some surfaces still send this action. -->
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <!-- WorkManager's SystemForegroundService for download notifications -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:node="merge" />
    </application>
</manifest>

